apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: yts-api
  labels:
    app.kubernetes.io/name: yts-api
spec:
  replicas: 4
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: yts-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: yts-api
    spec:
      serviceAccountName: yts-runtime
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api
          image: ghcr.io/davidnguyen/yts-api:canary
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: yts-config
            - secretRef:
                name: yts-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1500m
              memory: 2Gi
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: artifacts
          persistentVolumeClaim:
            claimName: yts-artifacts-pvc
  strategy:
    canary:
      maxSurge: 1
      maxUnavailable: 0
      steps:
        - setWeight: 10
        - pause:
            duration: 2m
        - setWeight: 25
        - pause:
            duration: 3m
        - setWeight: 50
        - pause:
            duration: 5m
        - analysis:
            templates:
              - templateName: yts-api-success-rate
        - setWeight: 100
